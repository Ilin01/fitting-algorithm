This algorithm has the capability of finding accuratelly the y value corresponding to an x value of a graph of every shape as long as it represnts a function.

Feel free to use cython if the program is too slow.
